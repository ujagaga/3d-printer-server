#!/usr/bin/env bash

set -e

if [ "$EUID" -ne 0 ]; then
  echo "Run as root (sudo ./nginx-config)"
  exit 1
fi


echo "Running certbot for ${DOMAIN}..."
certbot --nginx -d "$DOMAIN"

echo "Done. Site: https://${DOMAIN} -> 127.0.0.1:${PORT}"
